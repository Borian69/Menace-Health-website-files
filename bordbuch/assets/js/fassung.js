/* Fassung dieser App. Muss zum Cache-Namen in sw.js passen —
   der Selbsttest prüft genau das.

   Bei jeder Änderung an einer Datei aus der SHELL-Liste in sw.js hier
   hochzählen, sonst behält ein installiertes Handy die alte Fassung. */

export const BUILD = 'v1';
